@model ERP_MVC.TipoCliente

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TipoCliente</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.IdTipoCliente)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IdTipoCliente)
            @Html.ValidationMessageFor(model => model.IdTipoCliente)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TipoCliente1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TipoCliente1)
            @Html.ValidationMessageFor(model => model.TipoCliente1)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
