@model ERP_MVC.Disponibilidade

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Disponibilidade</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.IdDisponibilidade)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IdDisponibilidade)
            @Html.ValidationMessageFor(model => model.IdDisponibilidade)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Disponibilidade1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Disponibilidade1)
            @Html.ValidationMessageFor(model => model.Disponibilidade1)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
