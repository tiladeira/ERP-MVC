@model ERP_MVC.FormaPagamento

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FormaPagamento</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.IdFormaPagamento)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IdFormaPagamento)
            @Html.ValidationMessageFor(model => model.IdFormaPagamento)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.FormaPagamento1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FormaPagamento1)
            @Html.ValidationMessageFor(model => model.FormaPagamento1)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
