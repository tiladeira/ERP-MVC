@model IEnumerable<ERP_MVC.FormaPagamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Criar Novo", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FormaPagamento1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FormaPagamento1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFormaPagamento }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFormaPagamento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFormaPagamento })
        </td>
    </tr>
}

</table>
